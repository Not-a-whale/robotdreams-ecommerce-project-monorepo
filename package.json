{
  "dependencies": {
    "@nestjs/mapped-types": "*"
  },
  "name": "ecommerce-shop-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "turbo": "^1.11.0"
  }
}
